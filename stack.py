
from collections import deque

stack = deque()

stack.append(1)

    # Stack 
        # pop :- pop the last  element         O(n|1)
        # peek                                O(1)
        # push :- push at the last position   O()
        # Deletion :-                         O(1)
        #search :-                            O(n)

    # Queue

# depue  :- remove  the first element      O(n)
#  enque :- add  new elment at last        O(n|1)
# peek | top : -                           O(1)

